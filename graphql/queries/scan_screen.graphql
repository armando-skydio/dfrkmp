query AppScan($orgid: String!) {
  organization(uuid:$orgid) {
    name
    scans {
        edges {
            node {
                output {
                name
                photoCount
                }
            }
        }
    }
  }
}
